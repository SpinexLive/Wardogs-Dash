<article class="roster-event-card"><p class="eyebrow">Scheduled Match</p><h2><%= event.name %></h2><p class="event-date"><%= formatTime(event.startTime) %></p><footer><span><strong><%= event.signups %></strong> signed up</span><a class="action-button action-button--primary" href="/roster/<%= event.id %>"><%= event.hasRoster ? 'Edit Roster' : 'Create Roster' %></a></footer></article>
